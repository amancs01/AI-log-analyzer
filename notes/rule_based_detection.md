# Rule-Based Detection

Rule-based detection is a simple cybersecurity technique where suspicious activity is detected using predefined rules.

## Example

If a log line contains:

```txt
brute force
 
 then the analyzer can mark it as suspicious